using System;

namespace TicTacToe.Business
{
    public class GameBoard
    {
        public void newBoard() {
            private enum CellState
        {
            EMPTY,
            X,
            O
        }
        private var grid = new List<KeyValuePair<int, enum>>
            {
                for (int i = 0, int<10, int++){
                    new KeyValuePair<int, string>(i, CellState.EMPTY),
                }
            }
        }
    }
}